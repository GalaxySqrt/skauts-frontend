import { Routes } from '@angular/router';
import { PlayersPrizeListComponent } from './list/players-prize-list.component';

export const PRIZES_ROUTES: Routes = [
    { path: '', component: PlayersPrizeListComponent }
];
